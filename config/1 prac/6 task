nano checker
_______________________________________________________________
line=$(head -1 $1)
if [[ $line == "//"*]] || {{ $line == "#"* ]]; then
echo "В первой строке есть комментарии"
else 
echo "В первой строке нет комментариев"
fi
_______________________________________________________________
chmod +x checker
./checker (файл)
